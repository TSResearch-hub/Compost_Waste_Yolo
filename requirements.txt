# TOUT le projet s'installe avec ce seul fichier, depuis la racine du dépôt :
#     pip install -r requirements.txt
# La ligne -e installe aussi le paquet compost_detection (src/, scripts
# d'entraînement, interface de production) dans le même environnement.
-e .[app]

# Interface d'annotation (app.py)
streamlit==1.59.1
ultralytics
opencv-python
Pillow
numpy
matplotlib
packaging

# Serveur de la version mobile (interfaces/mobile/server.py)
fastapi
uvicorn[standard]
python-multipart

# Tests (interfaces/tests/) : python -m pytest interfaces/tests/
pytest
httpx

streamlit==1.40.0
ultralytics==8.4.107   # même version que compost-yolo/pyproject.toml (résultats reproductibles)
opencv-python-headless
Pillow
numpy
matplotlib
packaging
pathlib

# Serveur de la version mobile (mobile/server.py)
fastapi
uvicorn[standard]
python-multipart

# Tests (tests/) : python -m pytest tests/
pytest
httpx

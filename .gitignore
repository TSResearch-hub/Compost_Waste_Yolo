# Environnements Python
venv/
__pycache__/
*.pyc
*.pyo

# Dépendances npm (se recrée avec yarn install dans bbox_editor/frontend/)
interfaces/annotation/bbox_editor/frontend/node_modules/

# Assets lourds non versionné
images/
video/

# Données annotées (décommenter si tu ne veux pas les versionner)
# dataset_recolte/

# Archives lourdes du dataset (1 Go+) — ne jamais les committer
dataset_recolte.zip
*.zip

# Fichiers parasites Windows/WSL (métadonnées de téléchargement)
*Zone.Identifier

# Cache des miniatures du serveur mobile
interfaces/mobile/.thumb_cache/

# Exports d'entraînement (copies du dataset, volumineux) et runs YOLO
exports/
runs/**
!runs/.gitkeep
# Données annotées (non versionnées : volumineuses, vivent dans datasets/)
dataset_recolte/

# Données d'entraînement (volumineuses, régénérables) — seule l'arborescence
# (.gitkeep) est versionnée
data/**
!data/**/
!data/**/.gitkeep
crop_preview/

# Poids de modèles : jamais sur GitHub (models/ circule par Drive)…
*.pt
*.onnx
*_ncnn_model/
# …sauf le modèle DÉPLOYÉ des interfaces (66 Mo, sous la limite GitHub)
!weights/best.pt

# Caches Python / outils
*.egg-info/
.pytest_cache/
.ipynb_checkpoints/
